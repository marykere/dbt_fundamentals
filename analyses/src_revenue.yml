
version: 2

sources:
  - name: jaffle_shop
    description: A clone of a Postgres application database.
    database: raw
    schema: stripe
    tables:
      - name: payment
        description: a table of all order payments
        columns:
          - name: id
            data_tests:
              - unique
              - not_null